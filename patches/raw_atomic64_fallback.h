#ifndef _RAW_ATOMIC64_FALLBACK_H
#define _RAW_ATOMIC64_FALLBACK_H

/*
 * Fallback definitions removed as they conflict with
 * static inline versions provided elsewhere (e.g., atomic-raw64-compat.h)
 * and were non-atomic/incorrect.
 */

#endif // _RAW_ATOMIC64_FALLBACK_H
